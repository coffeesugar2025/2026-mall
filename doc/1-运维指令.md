# 中间件安装步骤

## 一、mysql安装步骤

### 准备前工作

1、检查是否已经安装过mysql ：  rpm -qa | grep mysql

如果环境中有遗留mysql则执行删除命令：rpm -e --nodeps mysql-xxxxxxxxx

2、查询遗留的mysql设置或命令，执行两条命令：

whereis mysql

find / -name mysql

如通过上述两条命令发现有遗留，则执行清除命令，将所有查到的mysql都删除感觉 ：rm -rf xxx xxx  

### 安装工作

```
```

1、检查mysql用户组和用户是否存在，如果没有，则创建，有的话可以不用添加，如果已经忘记密码等则保险期间还是删除重新创建吧

查看是否存在mysql用户的命令：

cat /etc/group | grep mysql
cat /etc/passwd |grep mysql

增加 mysql 用户的命令：

groupadd mysql
useradd -r -g mysql mysql

3、开始解压、安装

tar -xvf mysql-8.0.20-linux-glibc2.12-x86_64.tar.xz

mv mysql-8.0.20-linux-glibc2.12-x86_64  /usr/local/mysql

4、更改mysql目录下所有的目录及文件夹所属的用户组和用户，以及权限

mysql文件夹要配置为755的权限，如果不是则执行命令：

chmod -R 755 /usr/local/mysql

5、找一个目录来存储mysql的数据，如果你的数据量大，记得要找个大点的盘和目录，我的是在/data目录下

mkdir -p /data/mysql

此时一般网友的操作步骤是给这个目录设置了mysql权限：chown mysql:mysql -R /data/mysql

我在执行和调试过程中，遇到了mysql.sock无法找到和其他无法写的权限等问题，所以为给这个目录权限设置为：chmod 777 /data/mysql  

4、设置配置文件，编译/etc/my.cnf ， 内容如下

```
[mysqld]
#datadir=/var/lib/mysql
#socket=/var/lib/mysql/mysql.sock
# Disabling symbolic-links is recommended to prevent assorted security risks
#symbolic-links=0
# Settings user and group are ignored when systemd is used.
# If you need to run mysqld under a different user or group,
# customize your systemd unit file for mariadb according to the
# instructions in http://fedoraproject.org/wiki/Systemd
bind-address=127.0.0.1
port=3306
user=mysql
basedir=/usr/local/mysql
datadir=/data/mysql
socket=/tmp/mysql.sock
#socket=/data/mysql/mysql.sock
log-error=/data/mysql/mysql.err
pid-file=/data/mysql/mysql.pid
##character config
character_set_server=utf8mb4
symbolic-links=0
explicit_defaults_for_timestamp=true

[mysqld_safe]
#log-error=/var/log/mariadb/mariadb.log
log-error=/data/mysql/mariadb.log
#pid-file=/var/run/mariadb/mariadb.pid
pid-file=/data/mysql/mariadb.pid

#
# include all files from the config directory
#
!includedir /etc/my.cnf.d
```

5、初始化mysql,一定要记住初始化输出日志末尾的密码（数据库管理员临时密码）

./mysqld --initialize --user=mysql --datadir=/data/mysql --basedir=/usr/local/mysql

如果屏幕上没有打印则去日志文件中查看初始化完成后的临时密码：

cat /data/mysql/mysql.err

 6、测试启动mysql服务器

先将mysql.server放置到/etc/init.d/mysql中

```
cp /usr/local/mysql/support-files/mysql.server /etc/init.d/mysql

service mysql start
ps -ef | grep mysql
```

7、修改初始密码

首先登录mysql，密码是上述初始化时，日志最后一行生成的那个临时密码
./mysql -u root -p #bin目录下
./mysql -uroot -h 127.0.0.1 -p

怎么办呢，经过验证，需要使用alert命令去修改,执行命令：

ALTER USER 'root'@'localhost' IDENTIFIED BY 'xxxxxxx';
FLUSH PRIVILEGES;

9、开放远程连接（开通后你的mysql就可以让其他远端客户端访问了），执行命令：

```
use mysql;

update user set user.Host='%' where user.User='root';

flush privileges;

Grant all privileges on *.* to 'root'@'%' identified by ‘password’with grant option;

```

到此，你的mysql已经安装成功，开心的使用去吧。

## 二、Nacos安装

1，创建数据库

```
CREATE DATABASE nacos CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2，启动服务

```
sh startup.sh -m standalone
```

## 三、RocketMQ安装

1、vim start-nameserver.sh

```
#!/bin/bash
nohup sh /root/rocketmq/rocketmq-all-4.9.4-bin-release/bin/mqnamesrv &
```



2、vim start-rocketmq.sh

```
#!/bin/bash
nohup sh /root/rocketmq/rocketmq-all-4.9.4-bin-release/bin/mqbroker -c /root/rocketmq/rocketmq-all-4.9.4-bin-release/conf/broker.conf -n 10.181.128.43:9876 autoCreateTopicEnable=true &
```

3、touch /usr/lib/systemd/system/nameserver.service

```
[Unit]
Description=nameserver
After=network.target

[Service]
Type=simple
User=root
Group=root

ExecStart=/root/rocketmq/start-nameserver.sh

# Give a reasonable amount of time for the server to start up/shut down
TimeoutSec=300

# Place temp files in a secure directory, not /tmp
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

4、touch /usr/lib/systemd/system/rocketmq.service

```
[Unit]
Description=rocketmq
After=network.target

[Service]
Type=simple
User=root
Group=root

ExecStart=/root/rocketmq/start-rocketmq.sh

# Give a reasonable amount of time for the server to start up/shut down
TimeoutSec=300

# Place temp files in a secure directory, not /tmp
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

5、开机启动

```
systemctl daemon-reload && systemctl start nameserver.service && systemctl enable nameserver.service && systemctl start rocketmq.service && systemctl enable rocketmq.service
```



6，手工启动脚本

```
nohup sh /root/rocketmq/rocketmq-all-4.9.4-bin-release/bin/mqbroker -c /root/rocketmq/rocketmq-all-4.9.4-bin-release/conf/broker.conf -n 10.181.128.43:9876 autoCreateTopicEnable=true &
```



## 四、Redis安装

## 五、minio安装

1、下载地址

```
https://dl.minio.io/server/minio/release/darwin-arm64/archive/

历史版本下载地址
https://dl.minio.org.cn/server/minio/release/linux-amd64/archive/
版本信息：minio.RELEASE.2023-02-27T18-10-45Z
```

2、vim start-minio.sh

```
#!/bin/bash
export MINIO_ROOT_USER=admin
export MINIO_ROOT_PASSWORD=12345678
nohup /root/minio-server/minio server --console-address :9001 --address :9000 /root/minio-server/data >minio.log 2>&1 &
```

3、touch /usr/lib/systemd/system/minio.service

```
[Unit]
Description=Minio server
After=network.target

[Service]
Type=simple
User=root
Group=root

ExecStart=/root/minio-server/start-minio.sh

# Give a reasonable amount of time for the server to start up/shut down
TimeoutSec=300

# Place temp files in a secure directory, not /tmp
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

4、开机启动

```
systemctl daemon-reload && systemctl start minio.service && systemctl enable minio.service
```



6、访问地址

http://10.181.128.44:9001/login

## 六、Java安装

设置Java环境变量

```
export JAVA_HOME=/root/jdk-17.0.1
export PATH=$PATH:$JAVA_HOME/bin
export CLASSPATH=.:$JAVA_HOME/lib
```

## 七、常见执行指令

shig-file 打包指令

```
#!/bin/bash
rm -rf  /root/shig-file/shig-file.log
kill -9 $(ps -ef | grep shig-file | grep -v grep | awk '{print $2}')
nohup java -Xms512m -Xmx512m -XX:+UseG1GC -jar shig-file-service-1.0.0-SNAPSHOT.jar  > /root/shig-file/shig-file.log 2>&1 &
```

前端打包流程

```
#!/bin/bash

# 获取当前日期
currentTime=$(date "+%Y%m%d%H%M%S")

# 定义镜像版本
IMAGE_REPO="harbor.ccse.ctyun.cn:32000/hrtest/shig-config"
IMAGE_VERSION="v1.0.1-${currentTime}"
IMAGE_TAG="${IMAGE_REPO}:${IMAGE_VERSION}"
LATEST_TAG="${IMAGE_REPO}:latest"
HARBOR_PASSWORD="AAAaaa1234"

# 登录到Harbor仓库
echo "登录到Harbor仓库..."
docker login harbor.ccse.ctyun.cn:32000 -u ciicuser -p "$HARBOR_PASSWORD" || { echo "登录失败"; exit 1; }

# 构建Docker镜像
echo "开始构建镜像..."
docker build -f ./Dockerfile . -t "$IMAGE_TAG"  || { echo "构建失败"; exit 1; }

# 推送镜像
echo "开始推送镜像..."
docker push "$IMAGE_TAG" || { echo "推送失败"; exit 1; }

# 登出Harbor仓库
echo "登出Harbor仓库..."
docker logout harbor.ccse.ctyun.cn:32000

echo "镜像构建和推送完成: $IMAGE_TAG"
```

带环境变量的启动指令

```
export JAVA_HOME=/d/app/Java/jdk-17
export VHR_NACOS_IP="10.181.129.45"
export VHR_NACOS_PORT="8848"
export VHR_NACOS_USERNAME="nacos"
export VHR_NACOS_PASSWORD="nacos"
export VHR_NACOS_NAMESPACE="vhr-dev"
export VHR_REDIS_IP="10.181.128.15"
export VHR_REDIS_PORT="20260"
export VHR_REDIS_PASS="Shig@~2025"


java -jar main-data-push-service-1.0.0-SNAPSHOT.jar
```

docker-file

```
FROM harbor.ccse.ctyun.cn:32000/hrtest/openjdk:17-sw 
WORKDIR /home
ENV APP_NAME="shig-query-service-1.0.0-SNAPSHOT.jar"
ENV JAVA_OPTS="-Xms1024m -Xmx1024m -XX:+UseG1GC"
RUN /bin/cp /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
RUN echo "Asia/Shanghai" > /etc/timezone
ADD ./${APP_NAME} /home
ENTRYPOINT java ${JAVA_OPTS} -Dfile.encoding=utf-8 -Djava.security.egd=file:/dev/./urandom -jar /home/${APP_NAME}
```

push.sh

```
#!/bin/bash

# 获取当前日期
currentTime=$(date "+%Y%m%d%H%M%S")

# 定义镜像版本
IMAGE_REPO="harbor.ccse.ctyun.cn:32000/hrtest/shig-query"
IMAGE_VERSION="v1.0.1-${currentTime}"
IMAGE_TAG="${IMAGE_REPO}:${IMAGE_VERSION}"
LATEST_TAG="${IMAGE_REPO}:latest"
HARBOR_PASSWORD="AAAaaa1234"

# 登录到Harbor仓库
echo "登录到Harbor仓库..."
docker login harbor.ccse.ctyun.cn:32000 -u ciicuser -p "$HARBOR_PASSWORD" || { echo "登录失败"; exit 1; }

# 构建Docker镜像
echo "开始构建镜像..."
docker build -f ./docker-file . -t "$IMAGE_TAG"  || { echo "构建失败"; exit 1; }

# 推送镜像
echo "开始推送镜像..."
docker push "$IMAGE_TAG" || { echo "推送失败"; exit 1; }

# 登出Harbor仓库
echo "登出Harbor仓库..."
docker logout harbor.ccse.ctyun.cn:32000

echo "镜像构建和推送完成: $IMAGE_TAG"

```

yml文件

```
---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  annotations:
    deployment.kubernetes.io/revision: "36"
  name: "shig-maindata-consume-service"
  namespace: "ccse-ns-hrtest"
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      name: "shig-maindata-consume-service"
  strategy:
    rollingUpdate:
      maxSurge: "25%"
      maxUnavailable: "25%"
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        app: ""
        ccse_app_name: ""
        name: "shig-maindata-consume-service"
        rollingredeploy.ccse.ctyun.cn/time: "27_Aug_2025_09_27_27_GMT"
        source: "CCSE"
    spec:
      containers:
      - env:
        - name: "VHR_NACOS_IP"
          value: "10.181.129.45"
        - name: "VHR_NACOS_PORT"
          value: "8848"
        - name: "VHR_NACOS_USERNAME"
          value: "nacos"
        - name: "VHR_NACOS_PASSWORD"
          value: "nacos"
        - name: "VHR_NACOS_NAMESPACE"
          value: "vhr-dev"
        - name: "VHR_REDIS_IP"
          value: "10.181.128.15"
        - name: "VHR_REDIS_PORT"
          value: "20260"
        - name: "VHR_REDIS_PASS"
          value: "Shig@~2025"
        - name: "VHR_ROCKETMQ_IP"
          value: "10.181.128.191:9001;10.181.128.179:9001;10.181.128.248:9001"
        image: "harbor.ccse.ctyun.cn:32000/hrtest/shig-maindata-consume:v1.0.1-20250828090530"
        imagePullPolicy: "Always"
        name: "shig-maindata-consume-service"
        resources:
          limits:
            cpu: "4"
            memory: "8Gi"
          requests:
            cpu: "4"
            memory: "8Gi"
        terminationMessagePath: "/dev/termination-log"
        terminationMessagePolicy: "File"
      dnsPolicy: "ClusterFirst"
      imagePullSecrets:
      - name: "ciicuser"
      restartPolicy: "Always"
      schedulerName: "default-scheduler"
      securityContext: {}
      terminationGracePeriodSeconds: 30
```

k8s相关指令

```
kubectl logs -n ccse-ns-hrtest -f  `kubectl get pods -n ccse-ns-hrtest|awk '{print $1}'|grep consume`

kubectl delete pods -n ccse-ns-hr-fat   `kubectl get pods -n ccse-ns-hr-fat|awk '{print $1}'`

```

ingress样例

```
---
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/client-body-buffer-size: "100m"
    nginx.ingress.kubernetes.io/keepalive-timeout: "360"
    nginx.ingress.kubernetes.io/proxy-body-size: "200m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "360"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "360"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "360"
    nginx.ingress.kubernetes.io/rewrite-target: "/$1"
    nginx.ingress.kubernetes.io/upstream-keepalive-timeout: "360"
  creationTimestamp: "2025-09-04T02:11:48Z"
  generation: 1
  labels:
    app: "vhr-frontend"
  name: "vhr-frontend-ingress"
  namespace: "ccse-ns-hrtest"
spec:
  rules:
  - host: "hrsit.sdhi.com"
    http:
      paths:
      - backend:
          service:
            name: "vhr-res-frontend"
            port:
              number: 80
        path: "/mfe-res/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-paas-flow-frontend"
            port:
              number: 80
        path: "/mfe-flow/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-rule-frontend"
            port:
              number: 80
        path: "/mfe-rule/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-sys-frontend"
            port:
              number: 80
        path: "/mfe-sys/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-paas-perm-frontend"
            port:
              number: 80
        path: "/mfe-perm/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-salary-frontend"
            port:
              number: 80
        path: "/mfe-salary/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-personnel-frontend"
            port:
              number: 80
        path: "/mfe-personnel/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-talent-frontend"
            port:
              number: 80
        path: "/mfe-talent/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-talentsupply-frontend"
            port:
              number: 80
        path: "/mfe-talentsupply/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-talentsupply-site-frontend"
            port:
              number: 80
        path: "/mfe-talentsupply-site/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-cdr-frontend"
            port:
              number: 80
        path: "/mfe-cdr/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-integrate-frontend"
            port:
              number: 80
        path: "/mfe-integrate/(.*)"
        pathType: "ImplementationSpecific"
      - backend:
          service:
            name: "vhr-ai-frontend"
            port:
              number: 80
        path: "/mfe-ai/(.*)"
        pathType: "ImplementationSpecific"
status:
  loadBalancer:
    ingress:
    - ip: "10.181.128.167"

```

yml文件样例

```
---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  annotations:
    deployment.kubernetes.io/revision: "39"
  name: "shig-maindata-consume-service"
  namespace: "ccse-ns-hrtest"
  resourceVersion: "15389988"
  uid: "5df4a0cb-3127-46dc-8a5b-23f8b3c7b08b"
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      name: "shig-maindata-consume-service"
  strategy:
    rollingUpdate:
      maxSurge: "25%"
      maxUnavailable: "25%"
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        app: ""
        ccse_app_name: ""
        name: "shig-maindata-consume-service"
        rollingredeploy.ccse.ctyun.cn/time: "5_Sep_2025_00_46_33_GMT"
        source: "CCSE"
    spec:
      containers:
      - env:
        - name: "VHR_NACOS_IP"
          value: "10.181.129.45"
        - name: "VHR_NACOS_PORT"
          value: "8848"
        - name: "VHR_NACOS_USERNAME"
          value: "nacos"
        - name: "VHR_NACOS_PASSWORD"
          value: "nacos"
        - name: "VHR_NACOS_NAMESPACE"
          value: "vhr-dev"
        - name: "VHR_REDIS_IP"
          value: "10.181.128.15"
        - name: "VHR_REDIS_PORT"
          value: "20260"
        - name: "VHR_REDIS_PASS"
          value: "Shig@~2025"
        - name: "VHR_ROCKETMQ_IP"
          value: "10.181.128.191:9001;10.181.128.179:9001;10.181.128.248:9001"
        - name: "JAVA_OPTS"
          value: "-Xms2g -Xmx4g -XX:+UseG1GC"
        image: "harbor.ccse.ctyun.cn:32000/hrtest/shig-maindata-consume:v1.0.1-20250828090530"
        imagePullPolicy: "Always"
        name: "shig-maindata-consume-service"
        resources:
          limits:
            cpu: "2"
            memory: "4Gi"
          requests:
            cpu: "2"
            memory: "4Gi"
        terminationMessagePath: "/dev/termination-log"
        terminationMessagePolicy: "File"
      dnsPolicy: "ClusterFirst"
      hostAliases:
        - ip: "10.181.128.24"
          hostnames:
          - "iam-dev.shig.com.cn"
        - ip: "10.186.32.10"
          hostnames:
          - "iam-uat-new.sinotruk.com"
      imagePullSecrets:
      - name: "ciicuser"
      restartPolicy: "Always"
      schedulerName: "default-scheduler"
      securityContext: {}
      terminationGracePeriodSeconds: 30
```

