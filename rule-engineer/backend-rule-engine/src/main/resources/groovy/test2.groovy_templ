import com.salary.calcengine.groovy.*
import groovy.transform.CompileStatic

//@CompileStatic
class %s implements GroovyRule2 {
    void run(Context2 context){
        if(context == null) return;
        %s //业务执行逻辑：可配置化
    }
}