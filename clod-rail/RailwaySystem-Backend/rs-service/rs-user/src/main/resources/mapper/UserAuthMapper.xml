<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rs.mapper.UserAuthMapper">

    <!-- 基础ResultMap映射 -->
    <resultMap id="BaseResultMap" type="com.rs.model.customer.User">
        <!-- 主键 -->
        <id column="id" property="id" jdbcType="BIGINT"/>

        <!-- 基础用户信息 -->
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="gender" property="gender" jdbcType="VARCHAR"/>
        <result column="birthday" property="birthday" jdbcType="TIMESTAMP"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="introduction" property="introduction" jdbcType="VARCHAR"/>
        <result column="icon" property="icon" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="real_name" property="realName" jdbcType="VARCHAR"/>
        <result column="id_card" property="idCard" jdbcType="VARCHAR"/>

        <!-- 继承自BaseModel的字段 -->
        <result column="create_by" property="createBy" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="BIGINT"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 根据用户名查找用户 -->
    <select id="findByUsername" resultMap="BaseResultMap">
        select id,
               username,
               password,
               gender,
               birthday,
               address,
               introduction,
               icon,
               email,
               phone,
               real_name,
               id_card,
               create_by,
               create_time,
               update_by,
               update_time
        from user
        where username = #{username}
    </select>

    <!-- 根据ID查询密码 -->
    <select id="queryPasswordById" resultType="java.lang.String">
        select password
        from user
        where id = #{id}
    </select>

    <!-- 更新用户密码 -->
    <update id="updateUserPassword">
        update user
        set password = #{password},
            update_time = now()
        where id = #{id}
    </update>

    <update id="updatePhone">
        update user
        set phone = #{newPhone}
        where id = #{id}
    </update>

    <update id="updateEmail">
        update user
        set email = #{newEmail}
        where id = #{id}
    </update>

    <!-- 注册用户 -->
    <insert id="registerUser" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, password, email, phone, real_name, id_card)
        values (#{username}, #{password}, #{email}, #{phone}, #{realName}, #{idCard})
    </insert>

</mapper>