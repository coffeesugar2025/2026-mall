<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rs.mapper.UserMapper">

    <!-- 基础ResultMap映射 -->
    <resultMap id="BaseResultMap" type="com.rs.model.customer.User">
        <!-- 主键 -->
        <id column="id" property="id" jdbcType="BIGINT"/>

        <!-- 基础用户信息 -->
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="gender" property="gender" jdbcType="VARCHAR"/>
        <result column="birthday" property="birthday" jdbcType="TIMESTAMP"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="introduction" property="introduction" jdbcType="VARCHAR"/>
        <result column="icon" property="icon" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="real_name" property="realName" jdbcType="VARCHAR"/>
        <result column="id_card" property="idCard" jdbcType="VARCHAR"/>

        <!-- 继承自BaseModel的字段 -->
        <result column="create_by" property="createBy" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="BIGINT"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <resultMap id="InfoResultMap" type="com.rs.model.dto.response.user.UserInfoResDTO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="gender" property="gender"/>
        <result column="birthday" property="birthday"/>
        <result column="address" property="address"/>
        <result column="introduction" property="introduction"/>
        <result column="icon" property="icon"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="real_name" property="realName"/> <!-- 数据库是 real_name，DTO 是 realName -->
        <result column="id_card" property="idCard"/>     <!-- 数据库是 id_card，DTO 是 idCard -->
        <result column="create_time" property="createTime"/>
    </resultMap>

    <resultMap id="RegisterResltMap" type="com.rs.model.dto.response.user.UserRegisterResDTO">

    </resultMap>

    <update id="updateUser">
        update user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="introduction != null">
                introduction = #{introduction},
            </if>
            <if test="icon != null">
                icon = #{icon},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="realName != null">
                real_name = #{realName},
            </if>
            <if test="idCard != null">
                id_card = #{idCard},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateBy != null">
                update_by = #{updateBy},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="createBy != null">
                create_by = #{createBy},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="queryById" resultMap="BaseResultMap">
        select id,
               username,
               password,
               gender,
               birthday,
               address,
               introduction,
               icon,
               email,
               phone,
               real_name,
               id_card,
               create_by,
               create_time,
               update_by,
               update_time
        from user
        where id = #{id}
    </select>

    <select id="queryInfoById" resultMap="InfoResultMap">
        select id,
               username,
               gender,
               birthday,
               address,
               introduction,
               icon,
               email,
               phone,
               real_name,
               id_card,
               create_time
        from user
        where id = #{id}
    </select>
    <select id="queryUsername" resultType="java.lang.String">
        select username
        from user
        where id = #{id}
    </select>

    <select id="queryUsers" resultMap="InfoResultMap">
        select id,
               username,
               gender,
               birthday,
               address,
               introduction,
               icon,
               email,
               phone,
               real_name,
               id_card,
               create_time
        from user
        <where>
            <if test="username != null and username != ''">
                and username like concat('%', #{username}, '%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%', #{phone}, '%')
            </if>
            <if test="realName != null and realName != ''">
                and real_name like concat('%', #{realName}, '%')
            </if>
        </where>
        order by create_time desc
    </select>
</mapper>