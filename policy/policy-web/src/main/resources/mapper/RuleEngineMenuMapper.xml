<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.policy.web.store.mapper.PolicyMenuMapper">

    <select id="listMenuByUserId" resultType="com.policy.web.store.entity.PolicyMenu">
            select m.*
            from policy_menu m,
            policy_role r,
            policy_role_menu rm,
            policy_user_role ur
            where ur.user_id = #{userId}
            and ur.role_id = r.id
            and r.id = rm.role_id
            and rm.menu_id = m.id
            and m.deleted=0
            and ur.deleted=0
            and rm.deleted=0
    </select>
</mapper>
